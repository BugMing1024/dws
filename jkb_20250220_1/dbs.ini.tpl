;####dbs email和sms相关配置####
[${cwAppEnv}]
DBS.AppKey="${DBSAppKey}"
DBS.AppCode="${DBSAppCode}"
;email发送接口地址
DBS.Email.ApiUrl="${DBSEmailApiUrl}"
;sms发送接口地址
DBS.SMS.ApiUrl="${DBSSMSApiUrl}"
DBS.SMS.UserName="${DBSSMSUserName}"
